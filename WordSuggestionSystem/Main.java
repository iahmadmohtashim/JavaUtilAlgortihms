import java.util.*;public class Main {    public static void main(String[] args) {        WordSuggestionSystem suggestionSystem = new WordSuggestionSystem();        Scanner scanner = new Scanner(System.in);        // Training phase        System.out.println("Enter training text (type 'END' on a new line when finished):");        StringBuilder trainingText = new StringBuilder();        String line;        while (!(line = scanner.nextLine()).equals("END")) {            trainingText.append(line).append(" ");        }        suggestionSystem.train(trainingText.toString());        // Interactive suggestion phase        while (true) {            System.out.println("\nEnter a partial word for suggestions (or 'q' to quit):");            String input = scanner.nextLine().trim();            if (input.equalsIgnoreCase("q")) {                break;            }            // Get word completion suggestions            List<WordSuggestion> suggestions = suggestionSystem.getSuggestions(input);            System.out.println("Word completion suggestions:");            for (WordSuggestion suggestion : suggestions) {                System.out.println(suggestion);            }            // Get next word suggestions            List<WordSuggestion> nextWordSuggestions = suggestionSystem.getNextWordSuggestions(input);            System.out.println("\nNext word suggestions:");            for (WordSuggestion suggestion : nextWordSuggestions) {                System.out.println(suggestion);            }        }        scanner.close();    }}